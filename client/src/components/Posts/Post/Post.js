import React from 'react';

import useStyles from './styles.js'

const Post = () => {
    const classes = useStyles();
    return (
        <div>
            <h1>POST</h1>
        </div>
    );
};

export default Post;